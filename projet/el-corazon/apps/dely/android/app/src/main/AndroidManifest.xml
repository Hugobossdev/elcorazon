<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Permissions du livreur.
         Aucune n'etait declaree, et aucun greffon ne les apporte : le manifeste
         de `geolocator_android` ne contient qu'un service, pas une seule
         `uses-permission`. En release, `Geolocator.requestPermission()` refusait
         donc d'office, et le suivi ne demarrait jamais sur Android. -->

    <!-- Reseau. `firebase_messaging` declare deja INTERNET, mais le suivi n'a
         pas a dependre de la presence d'un greffon de notification. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <!-- Position. La precision fine est necessaire : un releve grossier
         (~1 km) ne dit rien d'utile a un client qui attend son repas, et le
         serveur ecarte de toute facon les releves a moins de 100 m du
         precedent. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>

    <!-- Le suivi doit survivre au passage en arriere-plan : un livreur roule,
         il ne regarde pas son telephone. Le service de premier plan de
         geolocator (declare par son propre manifeste) exige ces deux
         permissions, la seconde depuis Android 14.

         `ACCESS_BACKGROUND_LOCATION` n'y figure volontairement pas : un service
         de premier plan visible suffit au besoin, et cette permission-la
         demande une justification separee au Play Store. Suivre un livreur
         sans qu'il le voie n'est pas ce qu'on veut faire. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION"/>

    <!-- Android 13+. Sans elle, la notification de course proposee est
         silencieusement supprimee par le systeme. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- Ouverture de Google Maps / Waze / du composeur, depuis la fiche de
         course. Sans ce filtre, `canLaunchUrl` rend faux sur Android 11+ et
         chaque bouton de navigation retombe sur son message d'echec. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="geo"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.DIAL"/>
            <data android:scheme="tel"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.SENDTO"/>
            <data android:scheme="mailto"/>
        </intent>
    </queries>
    <application
        android:label="El Corazon Dely"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCtSGHbgwiNKhblSK7NpU7aVUvuxz-w-tM"/>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
