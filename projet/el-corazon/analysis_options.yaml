# Jeu de règles commun aux trois applications Flutter.
#
# Pourquoi ce fichier existe
# --------------------------
#
# Jusqu'au lot 5, chaque application décidait seule de ce qu'on lui demandait :
# `dely` n'activait **aucune** règle, `admin` en activait 7, `fastfood` 53. Les
# 56 diagnostics du 6 août 2026 se répartissaient en conséquence — 46 sur
# `dely`, 9 sur `fastfood`, 1 sur `admin`. Ce n'était pas que `dely` fût moins
# soigné : c'est qu'on ne lui demandait rien.
#
# Un jeu par application, c'est aussi la même triplication que celle que le plan
# de refactoring combat partout ailleurs : trois listes de 53 règles à tenir en
# phase à la main. Les trois `analysis_options.yaml` incluent donc celui-ci et
# ne gardent que ce qui leur est propre.
#
# Le jeu retenu est celui de `fastfood`, le plus exigeant des trois, plutôt
# qu'une intersection : aligner par le bas aurait rendu la mesure inutile.
#
# Voir `docs/refactoring-2026-08.md` §3.4 et §4 (lot 5).

include: package:flutter_lints/flutter.yaml

linter:
  rules:
    # Style
    prefer_single_quotes: true
    prefer_const_constructors: true
    prefer_const_literals_to_create_immutables: true
    prefer_const_declarations: true
    prefer_final_fields: true
    prefer_final_locals: true
    prefer_final_in_for_each: true

    # Prévention d'erreurs
    avoid_print: true
    avoid_empty_else: true
    avoid_unnecessary_containers: true
    avoid_relative_lib_imports: true
    avoid_types_as_parameter_names: true

    # Bonnes pratiques
    always_declare_return_types: true
    always_put_required_named_parameters_first: true
    always_use_package_imports: true
    annotate_overrides: true
    avoid_dynamic_calls: true
    avoid_function_literals_in_foreach_calls: true
    avoid_init_to_null: true
    avoid_null_checks_in_equality_operators: true
    avoid_redundant_argument_values: true
    avoid_renaming_method_parameters: true
    avoid_shadowing_type_parameters: true
    avoid_single_cascade_in_expression_statements: true
    avoid_slow_async_io: true
    avoid_type_to_string: true
    avoid_web_libraries_in_flutter: true
    cancel_subscriptions: true
    close_sinks: true
    prefer_adjacent_string_concatenation: true
    prefer_collection_literals: true
    prefer_conditional_assignment: true
    prefer_contains: true
    prefer_if_null_operators: true
    prefer_inlined_adds: true
    prefer_interpolation_to_compose_strings: true
    prefer_is_empty: true
    prefer_is_not_empty: true
    prefer_is_not_operator: true
    prefer_iterable_whereType: true
    prefer_null_aware_operators: true
    prefer_spread_collections: true
    prefer_typing_uninitialized_variables: true
    prefer_void_to_null: true
    require_trailing_commas: true
    sort_child_properties_last: true
    unawaited_futures: true
    use_build_context_synchronously: true
    use_key_in_widget_constructors: true
    use_late_for_private_fields_and_variables: true
    use_named_constants: true
    use_raw_strings: true
    use_string_buffers: true
    use_test_throws_matchers: true
